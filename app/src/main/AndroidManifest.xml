<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Install-time, no user prompt: needed to register the default-network callback that
         resets the cleanup waterfall cursor on SSID/VPN changes (#61, ADR-0001). -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Install-time, no user prompt (M6 audit, 2026-08-26): holds a bounded PARTIAL_WAKE_LOCK
         ("ramblr:transcription") across the recording+transcription window so a screen-off long
         dictation isn't CPU-throttled mid local decode. Acquired when recording starts, released
         when the pipeline reaches a terminal state (resetToIdle/onDestroy), with a hard acquire
         timeout as a leak backstop; see WhisperAccessibilityService.acquireTranscriptionWakeLock. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- #156 advanced tier: NEVER granted at install (signature|privileged|development), so this
         declaration is inert for every normal user. It exists so a power user can opt in once via
         `adb shell pm grant com.trevornk.ramblr android.permission.WRITE_SECURE_SETTINGS` (the
         Tasker pattern), which lets Ramblr manage its own OS accessibility-shortcut bindings
         (accessibility_button_targets / accessibility_shortcut_target_service) and re-enable its
         service in-app: raw Secure writes bypass the invisible-toggle "last shortcut off kills
         the service" sync that #156 is about. Feature-detected at every use
         (InvocationSecureSettings.canWrite); everything falls back to system-Settings deep links
         without it. tools:ignore because lint flags any non-app-grantable permission, which is
         exactly the point here. -->
    <uses-permission
        android:name="android.permission.WRITE_SECURE_SETTINGS"
        tools:ignore="ProtectedPermissions" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.PhoneWhisper">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Per-category Settings screens (#93 restructure): internal only, launched from
             MainActivity's category rows and never from outside the app.

             configChanges (#80 follow-up): a Pixel Fold's fold/unfold is a screenLayout +
             smallestScreenSize configuration change, same category as a rotation. Without this,
             Android's default behavior is to fully destroy and recreate the Activity: any
             transient in-memory UI state that isn't itself persisted (most visibly, an open
             AlertDialog like CloudProviderActivity's Add/Edit provider dialog) is lost, even
             though persisted SharedPreferences-backed settings correctly survive because they
             never lived in the destroyed Activity instance to begin with. Declaring these
             config changes here means Android reconfigures the existing Activity instance in
             place instead, so open dialogs (and anything else transient) survive a fold/unfold
             just like they'd survive backgrounding the app. -->
        <activity android:name=".SetupActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".TranscriptionActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".CleanupActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".StyleManagerActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".LivePreviewActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".CloudProviderActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".AdvancedActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <!-- Invocation-method chooser (#156): same internal-only + fold/unfold handling as the
             other per-category Settings screens above. -->
        <activity android:name=".InvocationActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <!-- Advanced's four sub-Activities (#104 restructure): mirror AdvancedActivity's own
             configChanges declaration above for the same Pixel Fold fold/unfold reasoning. -->
        <activity android:name=".OverlayAppearanceActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".BehaviorActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />
        <activity android:name=".DataLogsActivity" android:exported="false"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode" />

        <!-- "Type to fix" selection-menu entry point (#157 Option B): the user selects text in
             any app, taps Ramblr in the Copy/Paste popup, picks a style, and the cleaned text is
             returned to the host for replacement (or copied to the clipboard when the host says
             the field is read-only).

             Necessarily exported="true" — the platform's text-selection menu is what starts it,
             and it is only reachable through that explicit user gesture with the selection the
             user made. It is the first newly-exported Activity since MainActivity; every other
             Activity here stays exported="false".

             Deliberately NOT accessibility-backed: this Activity never touches
             WhisperAccessibilityService, which is the whole point of choosing this option in #157
             (the service's onAccessibilityEvent stays empty, so PRIVACY.md's claims stay true).

             android:label is what the selection menu shows, so it is the app name rather than a
             verb; excludeFromRecents/noHistory keep a one-shot dialog out of the recents list.
             configChanges mirrors the Settings Activities' Pixel Fold handling so an in-flight
             cleanup dialog survives a fold/unfold instead of being destroyed mid-call. -->
        <activity
            android:name=".ProcessTextActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PhoneWhisper.Transparent"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:configChanges="screenLayout|smallestScreenSize|screenSize|orientation|keyboardHidden|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Package-visibility decoys (Android 11+ declared package visibility,
                 https://developer.android.com/training/package-visibility).

                 Hosts building the text-selection toolbar call
                 queryIntentActivities(ACTION_PROCESS_TEXT, "text/plain") from their own
                 process, so the *host's* <queries> rules decide whether Ramblr's handler is
                 returned at all. Gmail, Google Messages and Keep do NOT declare a
                 PROCESS_TEXT <queries> entry — they only become able to see a handler app
                 when one of their generic <queries> intents (ACTION_VIEW variants) matches
                 some exported intent-filter in that app. ChatGPT/Kiwix appear in those
                 toolbars because their deep-link/file VIEW filters happen to match; Ramblr
                 (and Claude) had no matching filter, so the entry was silently filtered out
                 (device-verified on a Pixel 10 Pro Fold, Android 16).

                 The two filters below exist solely to match those generic VIEW queries.
                 They deliberately omit android.intent.category.DEFAULT, so implicit
                 intents can never resolve to this activity — Ramblr does not appear in any
                 link-open chooser (adb-verified) and the activity remains reachable only
                 explicitly. Hosts whose <queries> contain no VIEW intent at all (e.g.
                 SchildiChat) still cannot see any third-party handler; that is
                 host-app-side and unfixable from here. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
            </intent-filter>
        </activity>

        <!-- #156 dual-component design: the same service class ships as TWO components because
             flagRequestAccessibilityButton is static-XML-only (targetSdk > 29) yet decides the
             OS-side classification (TOGGLE vs INVISIBLE_TOGGLE) — so each "world" needs its own
             component, and exactly one is PM-enabled at a time (InvocationServiceMode).

             Component 1 (DEFAULT, enabled): WhisperAccessibilityService = "Floating icon" mode.
             Its config XML declares NO button flag -> ordinary TOGGLE service: independent
             Settings switch, floating ring only, no system button/gesture, no invisible-toggle
             trap. The name is deliberately unchanged from every prior release so existing
             installs' enabled_accessibility_services entries keep pointing at it — an update
             restores pre-#211 ring-only behavior with zero migration. -->
        <service
            android:name=".WhisperAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Component 2 (OPT-IN, android:enabled="false"): SystemControlsAccessibilityService =
             "System controls" mode. A trivial subclass (same behavior, same process-local
             statics) whose config XML DOES declare flagRequestAccessibilityButton, enabling the
             nav-bar/floating a11y button, the a11y gesture, and volume-keys-hold invocation —
             at the cost of the INVISIBLE_TOGGLE coupling (#156), which the guard-rail banner
             covers. Disabled by default so fresh installs and updates show exactly ONE "Ramblr"
             entry in system Settings; the Invocation screen's mode switch flips the two
             components via PackageManager.setComponentEnabledSetting (InvocationServiceMode).
             android:label stays the plain app name: only one component is ever PM-enabled at a
             time, so Settings never lists both, and the plain name reads cleanest in the
             shortcut UIs (a11y button chooser, volume-keys first-use dialog). -->
        <service
            android:name=".SystemControlsAccessibilityService"
            android:enabled="false"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config_system" />
        </service>

        <!-- Quick Settings tile (#127): alternative dictation trigger for users who hide the
             floating ring entirely. BIND_QUICK_SETTINGS_TILE + the QS_TILE intent-filter action
             are required by the platform for any TileService to be discoverable from the Quick
             Settings editor; icon/label are set programmatically in RamblrQsTileService so they
             stay in sync with recording state, but android:icon/android:label are also declared
             here since the system reads them once, before the service's first onStartListening,
             to populate the "add tile" editor row. -->
        <service
            android:name=".RamblrQsTileService"
            android:exported="true"
            android:icon="@drawable/ic_mic"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Voice-only keyboard (#143 Phase 3). Exported is required for the system IME manager
             to bind it; BIND_INPUT_METHOD is a signature permission, so arbitrary apps cannot.
             It is user-enabled/selected only through Android's supported input-method UI. -->
        <service
            android:name=".RamblrImeService"
            android:exported="true"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- ModelDownloadWorker promotes itself to a foreground service while a model download is
             in progress, so it needs a declared type (required on API 34+, #56). Overrides
             WorkManager's own manifest entry for this service. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- No-UI trampoline reached only via the "icon hidden" notification's own explicit
             PendingIntent (Feature B, IconVisibilityNotifications); never invoked from outside
             this app. Must be an Activity (not a BroadcastReceiver) so tapping the notification
             gets the normal, immediate shade-collapse animation instead of a lingering one. The
             translucent/no-history theme keeps it fully invisible and out of Recents. -->
        <activity
            android:name=".RestoreIconActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:excludeFromRecents="true"
            android:noHistory="true" />

        <!-- Exposes app-private files/benchmark_log.jsonl as a content:// URI to whatever app
             Trevor shares it via (Gmail, Discord, Drive, ...) from AdvancedActivity's "Share
             benchmark log" action (#100), without granting broader access to app-private
             storage. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/benchmark_log_file_paths" />
        </provider>

    </application>
</manifest>
